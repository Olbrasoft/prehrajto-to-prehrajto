__pycache__/
*.pyc
.venv/
venv/
.env
.env.local

# Logs and ephemeral state
# state/sync.log IS committed — it's the canonical batch history (same
# pattern as sledujtetocz-to-prehrajto). Only volatile temp files are
# ignored here.
state/*.tmp
/tmp/

# Local dev artifacts
.playwright-mcp/
*.swp
.DS_Store

# Raw uncompressed backlog (commit only the .gz version)
backlog/*.jsonl
